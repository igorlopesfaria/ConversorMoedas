<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.sympla.conversormoedas">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ConversorMoedasApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ConversorMoedas.Theme">
        <activity
            android:name=".splash.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ConversorMoedas.Theme.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".currency.CurrencyListActivity"
            android:label=""
            android:theme="@style/ConversorMoedas.Theme.NoActionBar" />
        <activity
            android:name=".exchange_rates.ExchangeRatesActivity"
            android:label=""
            android:theme="@style/ConversorMoedas.Theme.NoActionBar"/>

    </application>

</manifest>